\documentclass[border=10pt]{standalone}
\usepackage{pgfplots}
\pgfplotsset{compat=1.12}
\begin{document}
% --------------------------------------------------------------------------------
%                         parallelized
% --------------------------------------------------------------------------------
\begin{tikzpicture}
  \begin{axis}[
      title={TITLE}, %%% change this
      legend style={at={(1.5, 0.5)}, anchor=north, legend cell align=left, draw=none},
      symbolic x coords={1 core, 2 cores, 4 cores, 8 cores, 16 cores},
      xtick=data,
      x tick label style={rotate=45, anchor=east},
      ymin=0, ymax=MAX_TIME, %%% change this
      ylabel near ticks,
      yticklabel pos=right,
      ylabel={TIME_UNIT}, %%% change this
      grid = major,
      grid style=dashed,
  ]
   %%% rust-kzg with zkcrypto backend
   \addlegendentry{rust-kzg with zkcrypto backend}
   \addplot[mark=*, mark options={scale=1.5}, thick, red] coordinates {
      (1 core,   ZKCRYPTO_PARALLELIZED_1_CORES)
      (2 cores,  ZKCRYPTO_PARALLELIZED_2_CORES)
      (4 cores,  ZKCRYPTO_PARALLELIZED_4_CORES)
      (8 cores,  ZKCRYPTO_PARALLELIZED_8_CORES)
      (16 cores, ZKCRYPTO_PARALLELIZED_16_CORES) };
   %%% rust-kzg with blst backend
   \addlegendentry{rust-kzg with blst backend}
   \addplot[mark=*, mark options={scale=1.5}, thick, green] coordinates {
      (1 core,   BLST_PARALLELIZED_1_CORES)
      (2 cores,  BLST_PARALLELIZED_2_CORES)
      (4 cores,  BLST_PARALLELIZED_4_CORES)
      (8 cores,  BLST_PARALLELIZED_8_CORES)
      (16 cores, BLST_PARALLELIZED_16_CORES) };
   %%% rust-kzg with mcl backend
   \addlegendentry{rust-kzg with mcl backend}
   \addplot[mark=*, mark options={scale=1.5}, thick, black] coordinates {
      (1 core,   MCL_PARALLELIZED_1_CORES)
      (2 cores,  MCL_PARALLELIZED_2_CORES)
      (4 cores,  MCL_PARALLELIZED_4_CORES)
      (8 cores,  MCL_PARALLELIZED_8_CORES)
      (16 cores, MCL_PARALLELIZED_16_CORES) };

\end{axis}
\end{tikzpicture}
\end{document}
