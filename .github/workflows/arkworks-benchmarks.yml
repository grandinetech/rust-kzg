name: arkworks benchmarks

on: [push, pull_request, workflow_dispatch]

env:
  C_KZG_4844_GIT_HASH: 'a1d03803059471948e518820afa8518643e1b638'

jobs:
  benchmarks:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-11]

    steps:
      - uses: actions/checkout@v2

      - uses: actions/setup-java@v3
        with:
          distribution: "temurin"
          java-version: "11"

      - uses: actions/setup-go@v2
        with:
          go-version: ^1.19

      - name: "[arkworks] Benchmark"
        uses: actions-rs/cargo@v1
        with:
          command: bench
          args: --manifest-path arkworks/Cargo.toml

      - name: "[arkworks] Benchmark (parallel)"
        uses: actions-rs/cargo@v1
        with:
          command: bench
          args: --manifest-path arkworks/Cargo.toml --features parallel

      - name: "[arkworks] Benchmark (c-kzg-4844)"
        if: matrix.os == 'ubuntu-latest'
        run: |
          cd arkworks && bash run-c-kzg-4844-benches.sh

      - name: "[arkworks] Benchmark (c-kzg-4844 parallel)"
        if: matrix.os == 'ubuntu-latest'
        run: |
          cd arkworks && bash run-c-kzg-4844-benches.sh --parallel
